.header_room
  %ul
    %li マッチング一覧
    %li talk
.room_field
  .side
    .match_name
      - @user.each do |user|
        - if current_user.following?(user)
          = user.name
  .room
    .talk_room
      .talk
        talk
        